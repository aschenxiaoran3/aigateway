# 将本段纳入 server { ... }，root 指向 npm run build 产出的 dist 目录。
# 原理：先尝试精确文件，不存在则回退 index.html，供 React Router 接管。

location / {
    try_files $uri $uri/ /index.html;
}

# 若 API 与前端同域反代，请把 /api 写在 location / 之前，避免被 SPA 吃掉。
